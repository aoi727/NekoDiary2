# Assets

The application icons in `Sozai/` and `app-icon.ico` are original repository assets generated by `scripts/generate_icons.ps1`.

They were created specifically for this project as simple UI symbols and are intended to replace third-party decorative clip art with assets that are easier to track and maintain.

To regenerate them:

```powershell
.\scripts\generate_icons.ps1
```
